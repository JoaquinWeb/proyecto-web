<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="gep.list">
      <field name="name">gep list</field>
      <field name="model">gep.gep</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="gep.action_window">
      <field name="name">gep window</field>
      <field name="res_model">gep.gep</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="gep.action_server">
      <field name="name">gep server</field>
      <field name="model_id" ref="model_gep_gep"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="gep" id="gep.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="gep.menu_1" parent="gep.menu_root"/>
    <menuitem name="Menu 2" id="gep.menu_2" parent="gep.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="gep.menu_1_list" parent="gep.menu_1"
              action="gep.action_window"/>
    <menuitem name="Server to list" id="gep" parent="gep.menu_2"
              action="gep.action_server"/>
    -->
  </data>
</odoo>